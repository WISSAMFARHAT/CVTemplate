@using CVTemplate.Model;
@foreach (LanguagesModel Lang in LanguagesList)
{
    <div class="skills">
        <div class="--input">
            <span>Language</span>
            <input spellcheck="false" type="text" @bind-value="Lang.Title" />
        </div>
        @*<div class="--input">
            <span>Level</span>
            <div class="range">
                <input spellcheck="false" type="range" size="1" min="0" max="5" step="1" style="outline: none;" value="0" @onchange="(e)=> Change(e,Lang.ID)">
                <label>@Lang.RangeName</label>
            </div>
        </div>*@

        <div class="forms-done">
            <button class="--button _primary _icon" @onclick="()=>RemoveLang(Lang.ID)">
                <span>
                    <CVTemplate.Shared.Assets.Delete />
                </span>
            </button>
          @*  <button class="--button _secondary _icon" @onclick="()=>this.StateHasChanged()">
                <span>
                    <cvtemplate.shared.assets.correct />
                </span>
                <span>
                    done
                </span>
            </button>*@
        </div>
    </div>
}

